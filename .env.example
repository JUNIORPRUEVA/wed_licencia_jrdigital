# Archivo de ejemplo para variables de entorno
# Copiar a .env y configurar con valores reales

# === BACKEND CONFIG ===
NODE_ENV=development
PORT=4000

# === DATABASE ===
DATABASE_URL=postgresql://user:password@localhost:5432/fulltech_db

# === JWT AUTHENTICATION ===
# Generar con: node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
JWT_SECRET=your-jwt-secret-here-minimum-32-chars
JWT_REFRESH_SECRET=your-refresh-secret-here-minimum-32-chars

# === CORS ===
CORS_ORIGIN=http://localhost:3000

# === OFFLINE LICENSING (Ed25519 Keys) ===
# Generar con: npm run keygen
OFFLINE_SIGNING_KEY=your-private-key-base64
OFFLINE_VERIFICATION_KEY=your-public-key-base64

# === FRONTEND CONFIG ===
NEXT_PUBLIC_API_URL=http://localhost:4000
API_PROXY_TARGET=http://localhost:4000
